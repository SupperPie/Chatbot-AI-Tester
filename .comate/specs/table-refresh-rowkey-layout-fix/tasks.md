# Table Refresh / RowKey / Layout 修复任务清单

- [x] Task 1: 重构主数据与视图数据的状态管理，修复节点切换后数据残留
    - 1.1: 在 `app/ui/testcases.py` 中引入全量主数据集（如 `df_master`）并与筛选视图分离
    - 1.2: 确保分类筛选只作用于 `filtered_df/page_df`，禁止将筛选结果回写覆盖全量数据
    - 1.3: 在切换到 `root`（全部用例）时强制从全量主数据重建视图
    - 1.4: 节点变化时重置分页到第 1 页，避免页码越界造成空表或旧数据残留

- [x] Task 2: 恢复 ID 可重复语义并替换行定位键
    - 2.1: 在前端为每行生成稳定唯一的内部键（如 `__row_key`），不改变后端结构
    - 2.2: 将选择同步与编辑回写逻辑从 `id` 改为 `__row_key` 定位
    - 2.3: 移除依赖 `id` 唯一性的判断分支（例如 `main_mask.sum() == 1`）
    - 2.4: 保证 `id` 可重复显示（多轮 session）且不触发重复 key 报错

- [x] Task 3: 修复表格组件刷新键与缓存污染问题
    - 3.1: 调整 `st.data_editor` 的 widget key，纳入 `selected_category` 与分页/筛选签名
    - 3.2: 切换目录节点时确保表格组件状态与筛选结果同步刷新
    - 3.3: 在 root 场景禁用子节点视图复用路径，防止停留在上一节点数据

- [x] Task 4: 调整操作栏与分页栏布局及选择语义
    - 4.1: 将 Select All / Cancel ALL/ 统计信息 / 每页显示 / 分页器整合到同一行
    - 4.2: 缩小 Select All 与 Cancel ALL 按钮宽度
    - 4.3: 缩小“每页显示条数”选择器宽度
    - 4.4: 将 Select All 从“仅当前页”改为“全表（跨页）全选”
    - 4.5: 当前页全选改由 `st.data_editor` 的 Select 列 header 勾选能力承担
    - 4.6: 保持现有交互功能不回归（选择、分页、筛选仍可用）

- [x] Task 5: 回归验证并收敛修复结果
    - 5.1: 验证 root <-> 子节点反复切换时数据始终正确
    - 5.2: 验证重复 ID 场景下选择/编辑/分页行为正常
    - 5.3: 验证布局调整满足视觉要求
    - 5.4: 检查日志与控制台，确认无重复 key 或关键异常
