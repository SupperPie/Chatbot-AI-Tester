# DeepEval AI 接口测试管理平台 · 产品白皮书

> 让 AI 接口测试不再是流水线上的苦活，而是工程师的优雅体验。

---

## 一、平台定位

**专为 AI Agent / LLM 接口设计的全链路质量保障平台**

传统 API 测试工具（Postman / JMeter）面对 AI 接口的两大痛点束手无策：

1. **响应非确定性** — 同一输入每次回答都不同，硬比对失效
2. **流式 + 多模态** — SSE/NDJSON 协议、思考链、工具调用、业务结构化数据混杂

我们用一套 **"语义评分 + 结构化断言 + 多轮会话"** 三位一体的方案，把 AI 接口测试效率推到了新的高度。

---

## 二、量化的效率提升

### 2.1 测试编写阶段

| 工作内容 | 传统方式 | 本平台 | 效率提升 |
|---------|---------|--------|---------|
| 编写一条单轮用例 | 5-10 分钟（设计 + 编码 + 调试） | **30 秒**（自然语言描述 → AI 生成） | **10-20 倍** |
| 编写一条多轮用例 | 30 分钟+ | **2 分钟**（逐轮填表） | **15 倍** |
| 编写一组结构化断言（10 个字段） | 1-2 小时（手写代码 + 调试 JSONPath） | **3 分钟**（粘贴样本 + AI 一键生成） | **20-40 倍** |
| 复用已有断言到新用例 | 复制粘贴代码 | **绑定组件 ID（1 次点击）** | 无限倍 |

### 2.2 测试执行阶段

| 场景 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 50 条单轮用例（语义+断言） | 串行执行约 12-15 分钟 | **并行执行 6-8 分钟** | **~2x** |
| 100 条用例的报告页加载 | N+1 查询 ≈ 5 秒 | **批量查询 ≈ 50ms** | **100x** |
| 切换分类标签页 | 每次重新查询 ≈ 1 秒 | **缓存命中 ≈ 10ms** | **100x** |
| API 配置列表渲染 | 每次重读 ≈ 200ms | **缓存 ≈ 即时** | **20x** |
| 字段提取（深度嵌套 5 层） | 手写解析代码 100+ 行 | **JSONPath 一行** | 代码量 **-99%** |

### 2.3 维护阶段

| 工作内容 | 传统方式 | 本平台 |
|---------|---------|--------|
| 接口字段路径变更 | 改 N 处用例代码 | **改 1 个断言组件，所有引用自动生效** |
| 新增接口协议 | 写客户端 + 解析器 + 测试 ≈ 1 天 | **复用现有类型 + 配置 JSON ≈ 10 分钟** |
| 历史用例回归 | 重新跑全量 | **报告页右键 Rerun 单条/批量** |

---

## 三、详细功能介绍

### 3.1 测试用例管理（Test Case Management）

#### 界面布局示意

```
┌──────────────────────────────────────────────────────────────────┐
│  📋 Test Case Management                  [+ New] [Import] [...] │
├──────────────────────────────────────────────────────────────────┤
│  ◀ 分类树                  │  数据表格区                          │
│  ▼ 通用 (245)              │  ┌──────────────────────────────┐   │
│    ├ 单轮 (180)            │  │ ID │Input│Expected│🧩Assert│… │   │
│    └ 多轮 (65)             │  ├──────────────────────────────┤   │
│  ▼ 航班 (88)               │  │ TC1│查机票│...    │【3绑定】│… │   │
│    ├ 出票流程 (32)         │  │ TC2│改签  │...    │【绑定】 │… │   │
│    └ 改签退票 (56)         │  │ ...                          │   │
│  ▶ 酒店 (132)              │  └──────────────────────────────┘   │
│  ▶ 权益 (75)               │                                      │
│                            │  [Run Selected (Full ▼)] [Stop]      │
└──────────────────────────────────────────────────────────────────┘
```

#### 核心特性

- ✨ **分类树带数量徽章** — 一眼看到每个领域有多少用例
- ✨ **断言组件可视化绑定** — 点击 🧩 弹出 popup 选择已有组件
- ✨ **执行模式三选一** — Semantic（仅语义）/ Assertion（仅断言）/ Full（完整）
- ✨ **批量勾选执行** — 支持选中部分用例运行

📸 **建议截图位置**：
1. 分类侧边栏带数量徽章的展开状态
2. 断言绑定 Dialog 弹窗（多选 checkbox）
3. AI 一键生成用例的对话框

---

### 3.2 断言组件框架（Componentized Assertion Framework）

平台**首创**的可视化断言体系，分四层结构：

```
status_code（状态码）   structure（结构）    field（字段）       composite（组合）
     ▲                      ▲                  ▲                   ▲
     └──────────────────────┴──────────────────┴───────────────────┘
                                  断言组件库
                                       │
                                       ▼
                              测试用例引用绑定
```

#### 支持的判断条件

| 大类 | 条件 | 示例场景 |
|------|------|---------|
| **field** | equals / not_equals / contains / matches / in / not_empty / type / gt / gte / lt / lte / length | `data.flight_list[0].price < 5000` |
| **structure** | path_exists / required_fields / field_count / array_not_empty / nested_structure | `data.bundle_list 非空且长度 ≥ 1` |
| **status_code** | equals / in_range / not_equals | `200 ≤ code < 300` |
| **composite** | 嵌套引用其他组件 | "完整响应校验"组合 |

#### JSONPath 三级降级查找策略

```
用户输入: data.bundle_list[0].bundle_name
  │
  ├─ 1️⃣ 尝试绝对路径: $.data.bundle_list[0].bundle_name  ✓ 命中？
  │
  ├─ 2️⃣ 尝试递归搜索: $..bundle_name                     ✓ 命中？
  │
  └─ ❌ 抛 KeyError，告诉用户尝试过哪些路径
```

#### Scope 参数过滤 SSE 消息类型

```python
scope = "done"        # 只在 type=done 的消息中查找
scope = "token"       # 只在 token 流中查找
scope = ""            # 默认: 在合并响应上递归搜索（最宽松）
```

📸 **建议截图位置**：
1. 断言组件库页面（卡片式展示所有组件）
2. AI 一键生成断言的输入界面（左 JSON 样本 + 右自然语言描述）
3. AI 生成结果的勾选打包界面

---

### 3.3 双引擎评分系统

#### 并行执行架构

```
       ┌──────────────────────────┐
       │   API 调用（共享）       │
       │   提取 raw_data + lines  │
       └────────────┬─────────────┘
                    │
        ┌───────────┴───────────┐
        ▼ Thread A              ▼ Thread B
  ┌──────────────┐        ┌──────────────┐
  │ 断言引擎      │        │ DeepEval     │
  │ JSONPath     │        │ Correctness  │
  │ scope 过滤    │        │ Faithfulness │
  │ 组件递归执行 │        │ a_measure    │
  └──────┬───────┘        └──────┬───────┘
         │                       │
         └───────────┬───────────┘
                     ▼
              综合 passed 判定
```

#### 执行模式对比

| 模式 | 调用 LLM 评分 | 调用断言引擎 | 适用场景 |
|------|--------------|-------------|---------|
| **Semantic** | ✅ | ❌ | 关注回答质量、token 成本可控 |
| **Assertion** | ❌ | ✅ | 关注接口稳定性、零 token 成本 |
| **Full** | ✅ | ✅ | 上线前回归、最严格质量门 |

📸 **建议截图位置**：
1. 执行模式下拉框（带 emoji 区分）
2. 实时进度条 + 当前用例信息
3. 测试报告中的并行评分结果展示

---

### 3.4 多接口协议适配

#### 已内置 12 种接口客户端

```
┌────────────────────────────────────────────────────────────────┐
│  Internal APIs               External Platform APIs             │
│  ─────────────               ───────────────────────             │
│  • Bundle API                • Dify Chat (/v1/chat-messages)    │
│  • Skills API                • Dify Workflow                    │
│  • Flight API                • AI Engineering (Supervisor)      │
│  • Hotel API                                                    │
│  • Limo API                  Business APIs                      │
│  • Agent QA API              ────────────                       │
│  • Translation API           • Trip Planner (行程规划)          │
│  • Entitlements API          • 权益查询（中信/国内/国际）       │
└────────────────────────────────────────────────────────────────┘
```

每种协议都支持：

- ✅ SSE 流式（`data: ...` 前缀）
- ✅ NDJSON 多行 JSON
- ✅ 标准 JSON
- ✅ TTFT（首字节时延）测量
- ✅ Thinking Process 提取
- ✅ Inform Base（工具调用结果）提取
- ✅ Raw Data 完整保存

#### 添加新接口只需 3 步

```
1. Settings 页面 → 新增一行
   ┌─────────┬──────────────┬──────────────┐
   │ Name    │ URL          │ Type         │
   ├─────────┼──────────────┼──────────────┤
   │ 权益-中信│ http://...   │ entitlements │
   └─────────┴──────────────┴──────────────┘

2. 填 Request Params JSON 模板

3. 保存 → 立即可用 ✓
```

📸 **建议截图位置**：
1. Settings 页面的接口配置表格
2. API 下拉选择器（带 12 种接口）

---

### 3.5 测试报告（Test Report）

#### 多维度数据表格

```
┌───┬──────┬──────┬───────┬───────┬───────┬───────┬───────┬─────┐
│✓  │Case ID│Turn │Input  │Actual │Score  │Passed │Assert │TTFT │
├───┼──────┼──────┼───────┼───────┼───────┼───────┼───────┼─────┤
│☐  │ TC001│  1   │查机票 │上海...│ 0.85  │  ✅   │ 5/5✅ │1.2s │
│☐  │ TC002│  -   │改签   │需要...│ 0.42  │  ❌   │ 3/5❌ │0.8s │
│☐  │ TC003│  -   │取消   │好的...│ 0.95  │  ✅   │  -    │1.5s │
└───┴──────┴──────┴───────┴───────┴───────┴───────┴───────┴─────┘

  断言详情展开:
  ┌────────────────────────────────────────────┐
  │ ✅ 顶层结构验证: lob/data/content 都存在   │
  │ ✅ data.bundle_list 数组非空 (length=3)    │
  │ ❌ data.total > 0: 实际值 0                │
  │ ✅ status_code = 200                       │
  │ ✅ content 不包含 "error"                  │
  └────────────────────────────────────────────┘
```

#### 贴心设计

- ✨ **空列自动隐藏** — Thinking / Inform Base 全空时整列消失
- ✨ **Markdown 渲染** — 复杂 AI 输出友好展示
- ✨ **右键 Rerun** — 单条/批量重跑
- ✨ **PDF 一键导出** — 自动生成包含通过率、耗时分布的归档报告
- ✨ **历史趋势图** — 多次执行的通过率走势

📸 **建议截图位置**：
1. 报告主表格（含通过/失败彩色徽章）
2. 断言详情展开面板
3. PDF 报告样张（首页统计 + 详细列表）
4. 通过率历史趋势线图

---

### 3.6 后台任务调度（Job Manager）

```
                ┌──────────────────────┐
   用户点击 Run │  Job Manager         │
       ──────►│  (Background Thread) │
                │                      │
                │  ┌────────────────┐  │
                │  │ Job Queue      │  │
                │  ├────────────────┤  │
                │  │ • 50 cases     │◄─┼─── Worker Threads
                │  │ • 30 done      │  │     (并发执行)
                │  │ • 5 in progress│  │
                │  └────────────────┘  │
                │           ▲          │
                └───────────┼──────────┘
                            │
              UI 实时刷新 ──┘
              (进度条 + 当前用例)
```

#### 特性

- 🚀 **完全异步** — 关闭浏览器也继续跑
- 🚀 **中途可停** — 点 Stop 立即中断
- 🚀 **进度持久化** — 刷新页面无缝恢复
- 🚀 **失败可继续** — 单条失败不影响整批

---

## 四、技术栈与架构亮点

```
┌──────────────────────────────────────────────────────────────┐
│  前端: Streamlit 1.50 + streamlit-antd-components            │
│  ────────────────────────────────────────────────────        │
│  评分: DeepEval (GEval, Faithfulness, Conversational GEval)  │
│  断言: 自研引擎 + jsonpath-ng                                │
│  存储: PostgreSQL + JSONB                                    │
│  ORM:  SQLAlchemy 2.0                                        │
│  AI:   OpenAI Compatible (Qwen3-Max / DashScope)             │
│  并发: ThreadPoolExecutor + asyncio                          │
│  部署: Docker + Streamlit Server                             │
└──────────────────────────────────────────────────────────────┘
```

---

## 五、关怀设计细节合集

### ❤️ 给开发者的关怀

| 痛点 | 解决方案 |
|------|---------|
| Streamlit 默认 Ctrl+C 劫持复制 | `.streamlit/config.toml` 设 `toolbarMode=minimal` |
| pandas NaN 到处崩 | 全局 `_clean()` 函数过滤 `nan/none/null` |
| SQLAlchemy mapper 初始化失败 | `app/models/__init__.py` 集中导入所有模型 |
| 断言参数命名不一致 | `_normalize_params` 自动映射 `path→field`、`value→expected` |

### ❤️ 给测试人员的关怀

| 痛点 | 解决方案 |
|------|---------|
| 误删组件没救 | 二次确认 Dialog |
| 多轮会话调试难 | 自动注入 session_id，调试日志详细打印 payload |
| 找字段路径找半天 | JSONPath 三级降级，写啥都能找到 |
| AI 评分慢吃 token | Assertion-only 模式零 token 成本 |
| 测试结果难分享 | 一键 PDF 导出 |

### ❤️ 给产品经理的关怀

| 痛点 | 解决方案 |
|------|---------|
| 不懂代码看不懂结果 | 通过率徽章 + Markdown 渲染 + PDF 报告 |
| 历史趋势看不清 | 趋势图自动统计 |
| 跨用例复用规则难 | 组合断言 + 标签体系 |

---

## 六、推荐截图清单

| # | 页面 | 推荐场景 |
|---|------|---------|
| 1 | **首页 Dashboard** | 显示总用例数、最近一次通过率 |
| 2 | **测试用例管理** | 分类树展开 + 数据表格满屏 |
| 3 | **断言绑定 Dialog** | popup 弹窗的多选 checkbox |
| 4 | **AI 生成用例** | 输入需求 → 输出结果对比 |
| 5 | **AI 生成断言** | JSON 样本 + 自然语言 → 多张组件卡片 |
| 6 | **断言组件库** | 卡片视图、标签筛选、操作按钮 |
| 7 | **执行进度** | 进度条 + 当前用例 + Stop 按钮 |
| 8 | **测试报告主表** | 多列数据 + 通过/失败徽章 |
| 9 | **断言详情展开** | ✅/❌ 列表 + 失败原因 |
| 10 | **报告趋势图** | 历史通过率折线图 |
| 11 | **Settings 页面** | API 配置表格 + Request Params JSON |
| 12 | **PDF 导出样张** | 封面 + 统计 + 明细页 |

### 截图操作建议

1. 启动本地 Streamlit 应用：`streamlit run streamlit_app.py`
2. 准备一份**漂亮的 mock 数据**（30+ 条用例、10+ 断言组件、几次历史执行）
3. 按上面的"推荐截图清单"逐页截图（macOS 推荐 `Cmd+Shift+4` 精确选区）
4. 截图后用 [Carbon](https://carbon.now.sh/) 或 [CleanShot X](https://cleanshot.com/) 加阴影、圆角、背景
5. 把截图按章节插入文档对应位置

---

## 七、关键指标一览

```
┌──────────────────────────────────────────────────────────────┐
│  📊 平台核心指标                                              │
├──────────────────────────────────────────────────────────────┤
│  ⚡ 用例编写效率           ↑ 10-20x                          │
│  ⚡ 断言编写效率           ↑ 20-40x                          │
│  ⚡ 测试执行速度           ↑ 2x（并行评分）                  │
│  ⚡ 报告页加载速度         ↑ 100x（缓存 + 批量查询）         │
│  ⚡ 接口适配开发量         ↓ 99%（配置化代替编码）           │
│  ⚡ 字段提取代码量         ↓ 99%（JSONPath 代替解析器）      │
│  📦 已内置接口协议         12 种                              │
│  📦 已内置断言条件         20+ 种                            │
│  📦 已支持 LLM 模型        OpenAI 兼容协议（任意模型）       │
└──────────────────────────────────────────────────────────────┘
```

---

## 八、下一阶段路线图（Roadmap）

- 🔮 **多模型对比评测** — 同一批用例横向跑 GPT-4 / Claude / Qwen，自动出对比报告
- 🔮 **CI/CD 集成** — GitHub Actions / GitLab CI 触发回归
- 🔮 **断言市场** — 跨团队共享断言组件库
- 🔮 **告警 webhook** — 通过率跌破阈值自动通知
- 🔮 **A/B 测试支持** — 对比 prompt 变更前后的指标变化

---

> **DeepEval AI 接口测试管理平台** — 把 AI 测试从"经验主义"带入"工程化"。
> 让每一次模型迭代都有据可依，让每一次接口变更都有底气上线。
